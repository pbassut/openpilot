# /pull-request-fix {pull_request_number_or_link}

When this command is used ask the presenter agent to improve the PR provided as an argument by:

- Updating the description to make sure it matches what the code does.

# Output

- Respond with the changes made and why
